{
  "name": "spediak-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd Spediak && npm run dev",
    "start": "cd Spediak && npx expo start --web",
    "build": "cd Spediak && npm run build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
